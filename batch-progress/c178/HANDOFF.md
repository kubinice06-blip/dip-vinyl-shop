# c-178 交接（2026-09-22）：jp-1 線第六批，1979–1981，29 張

**這批可以接本機上傳了。** 雲端能做的全部做完，剩下的是雲端依 `REMOTE_RUNBOOK.md`
**不能做**的（KV、Firestore、`seed_cards.json`、`album_overrides`），不是沒做完。

## 一、37 張提案收 29 退 8（78%）

## 二、逐項對照 `ALBUM_ONBOARDING.md` 的完成標準

| 項目 | 狀態 |
|---|---|
| 身分（rgMbid 釘住） | **29/29 全部 pinned，零 §1 人工** |
| 三軸與頂點資格 | 卡單已帶；**本批 apex 0 張** |
| 固定簡介（desc） | **29/29 全 full**，字數 **204–238** |
| 封面 | **23/29** |
| 固定試聽／無來源狀態 | **25/29**，**4 張走固定無來源狀態** |
| §5.5／§5.6 例外欄位 | 例外欄全空 |
| published gate | 本機端 |

**4 張無來源**：`Native Son《Coast to Coast (Live in USA)》`、`Sunburst《Sunburst》`、
`アンリ菅野《Just Groovin'》`、`浅川マキ《ONE》`。
**6 張無封面**：`Coast to Coast`／`ONE`／`Silver Shadow`／`The Good Bad Girl`／`Marginal Love`／`Mountain`。

## 三、⚠ 本機審稿要盯的六件事

1. ⚠ ⚠ **`Coast to Coast` 與 `ONE` 是今天才改回無來源的**（主線第 1935-B 條）。
   探測層的別名交叉污染修掉之後重跑，這兩張原本各自配到一張**別的碟**
   （前者是蘇格蘭放克考古廠牌 Athens Of The North 的 2007 年同名碟，後者是《浅川マキの世界》1970）。
   **正文不受影響（本批本來就不寫聆聽入口），但上傳時的來源欄要照現在的狀態。**
2. ⚠ **`坂田明《Pochi》` 與 `Wha-ha-ha《Live-Dub》` 的來源是人工回撈救回來的**
   （成因 9 店面加括號補註、Apple 把 12 吋 45 轉兩軌 LP 標成 `- EP`）。
   **`previews.json` 裡這兩筆的 `recoveredBy` 欄非空——任何重跑都不准刪它們**（第 1935-B 條）。
3. ⚠ **`Wha-ha-ha《Live-Dub》` 正文寫成專輯、不寫 EP**（Apple 的 `- EP` 是店面標示錯誤）。
4. ⚠ **`赤尾三千子《風舞》` 取 1981**：Apple jp 逐字 1982-01-21／℗1982，MB 與 Discogs 都是 1981。
   **東芝EMI 盤的 Apple 日期只能佐證不能推翻**（主線第 1936-B 條）。
5. ⚠ **`森山威男カルテット《スマイル》` 與《Smile》是同一張碟的兩個沒折疊的 RG**
   （五軌題與時長逐毫秒相同、掛名不同、rgMbid 不同、盤名一羅馬字一片假名）。
   **`chk-prop` 與 `dedup` 六道全部抓不到這個形狀。** 本卡只收了一張。
6. ⚠ **`死ぬ時は別》` 音效欄的「赤塚不二雄」與漫畫家赤塚不二夫 不是同一人**
   （Discogs 藝人頁 profile 逐字註明）——**本卡一字沒寫。**

## 四、驗證（都是我自己重跑複核的，不是照代理的報告抄）

- `chk-prop` 兩組標記 0、`dedup-crossbatch c178` 跨批撞卡 0
- `qa-batch research/hooks/out c178` **三階段全部通過**、`chk-hook-crossgroup c178` 29 張全過
- `qa-check-research` 兩組標記 0、`fix-spacing` 兩檔待補 0
- **鉤子預算 29 筆我自己逐筆重算過**：216–230，**與代理報的 29 個數字逐格相符、零誤差**
- **desc 29 張 204–238**，`<180` 0 張、`>240` 0 張
- **跨兩組 29 張的 4-gram 我自己重掃**：≥3 張的 8 條**全是專名**
  （`日本コロムビア` 一族 4 條＋向井滋春／富樫雅彦／清水靖晃／千野秀一），**0 條句型同構**

## 五、三件給下一線的

1. **研究層推翻策展層 27 處**，八處人名改判仍是同一個機制：**從 Discogs 羅馬字逆推漢字**。
2. **三處「無來源的斷言」被退成不寫**（`杉本和也` 的漢字寫法、《Sunlight Whisper》八軌歸屬、
   《下駄はいてこなくちゃ》七軌歸屬）——**策展層的斷言沒有來源時，研究層的正確處置是「退成不寫」，
   不是「找個來源補上」。**
3. **鉤子層實測**：光是「動筆前配好日期＋動詞」不夠（配過了仍長出 17 條同構）。
   **根因是「字標＋號碼」這個尾巴只有三四種寫法**——**動詞表要連「目錄號怎麼引入句子」一起配。**
