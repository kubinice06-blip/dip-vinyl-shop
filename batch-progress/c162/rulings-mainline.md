# c-162 主線裁定

（`rulings.md` 由策展兩組使用，號段 1738–1789；主線的條目寫在本檔。）

## 第 1689 條（主線，**重要；容器重啟後學到的三件事，全部要改進派工詞**）

2026-09-19 容器重啟一次，**四支代理同時被殺**。損失不平均，差別正好把「續跑防呆該做到哪」量出來了：

| 代理 | 死時狀態 | 重派後 |
| --- | --- | --- |
| c-162 策展 a | **prop-a 已 17 筆、rulings 已 203 行在 repo 裡** | **接續只補第 18 張與 1744–1754 條，零重跑** |
| c-160 鉤子 a／b、c-162 策展 b | **一個檔都還沒落地** | **整支重來** |

### ⚠⚠ (一) 「產生大檔的證據層」必須當場寫進 repo（新立，本條最重要）

**c-162 策展 a 的前一棒抓過 Billboard，但只留在暫存目錄——容器一重啟就全沒了，卡單引得再逐字也救不回原文。**
**重派的那一棒只好整個重抓。**

**→ 派工詞的續跑防呆從兩句改成三句**：
1. 每做完 N 筆就把目前結果寫進輸出檔；
2. 開工時先檢查輸出檔是否已有部分內容，有就接續補完；
3. ⚠ **新增：任何「產生大檔的證據層」（紙本 OCR、批次抓取的原始資料）一抓完就立刻寫進 repo 並 append `SOURCES`，
   不要留在暫存目錄等交件時才搬。**

### ⚠⚠ (二) 重派時，前一棒的交件總表要當「待驗收的宣告」讀，不是事實

重派的那一棒對前一棒的第 1738 條訂正了三處：
**「18 張」實際只有 17 張、「紙本已入庫」實際沒有、「命中 6 張」實際 7 張。**
**它的做法正確**：**第 1738 條原文一字未改，訂正另寫在第 1752 條。**
**→ 這條寫進後續所有重派派工詞。**

### (三) 撞陳列的處數差不是錯

前一棒報 25 處／13 張、本棒報 19 處／13 張——**張數一致，處數差只是折鍵器要不要剝括號。**
**張數才是要對的那個數。**

## 第 1690-A 條（主線）：**c-162 策展 a 交件驗收——收 18 退 5、chk-prop 標記 0、年份改判 1**

`chk-prop a` **標記 0**（125 批 5,040 卡，跨批撞卡 0、三項只報不擋也全 0）；**rgMbid 18／18 對上 slice**。
**年份改判 1**：**Avishai Cohen《Seven Seas》2010→2011**（本棒補到**第五層**獨立佐證）。

⚠ **撞 apex 4 處，其中一處是新形狀**：
**`Jason Moran —《Ten》` 盤名逐字撞 seed 的 `Pearl Jam —《Ten》(1991)`（apex hall），`chk-prop` 四道全不亮**
——**與 c-161 b 抓到的 `Never Can Say Goodbye` 同族（盤名逐字相同、掛名不同）。**
**→ 這已經是第二次，不是巧合：`chk-prop` 的盲區清單要正式加一條「盤名撞 apex、掛名不同」。**
另曲目同源貼到 apex 3 組。

## 第 1691 條（主線）：**Billboard 已覆蓋 1955→2012；抓法有兩處要訂正**

**新增 `batch-progress/enum/billboard-bn-2010-2012-ocr.txt.gz`（148 期、939 命中頁、零失敗、3.8 MiB）。**

⚠⚠ **我派工詞裡的 URL 是錯的**：`Billboard/10s/<年>/` **不存在，三年全 404**
——**正確是 `Billboard/00s/<年>/`，2010–2019 全在 `00s/` 底下。**
⚠⚠ **而且 2010 以後不能再枚舉週六**（2011 只有 46 期、2013 起檔名形狀改了）
——**改走索引頁 `Billboard-Magazine.htm` 撈 URL，148 期一次到齊、不到一分鐘。**
**→ 這兩條要寫進後批派工詞；而且 2013 以後若要抓，一律走索引頁。**
⚠ **b 組與後批不要重下載 PDF——放寬關鍵字重掃那個 gz 即可。**

## 第 1692 條（主線）：**`Sunnyside` 確定是授權代理不是他廠母體**

**《Duende》美版 Discogs notes 逐字 `Under License From EMI Music France`**
——**本線第一次拿到硬證據。(丙) 從此對 `SSC` 目錄號直接過閘，不必再逐張查。**

## 第 1701 條（主線）：**c-162 策展 b 交件驗收——收 18 退 4、兩組 36 張 chk-prop 標記 0、年份改判 0**

**退 4 全部是 (乙)**（Duke Ellington 系列合輯、兩張 Turrentine 的 1979／1980 原盤、quasimode 精選）
——**(戊) 一筆都沒中**。**Monk《Paris 1969》判 (甲) 收**（Discogs format 三筆全乾淨、℗ 是瑞士 Laser Swing）。
**紙本 14／18**（本組 13 張美國盤，第 1664 條的判準再次成立），**8 個街頭日到手**。
**Billboard 現覆蓋 1955→2013。**

⚠ **`chk-prop` 第三道報的 1 筆是刻意的**：`Robert Glasper《Black Radio 2》↔ 線上池《Black Radio》`
——**那是第 1768 條裁定的預期輸出，只報不擋，不必處理。**

## 第 1702 條（主線，**`chk-prop` 的第七種盲區，新立**）：**標點字元不同形**

**slice 給 `Gov’t Mule`（U+2019 彎引號）、池中 seed 是 `Gov't Mule`（ASCII 直引號）
——折鍵後同鍵、四道全不亮。** 策展層已取 ASCII 形。

⚠⚠ **這一條與第 1620-U 條（我派工詞用直引號、輸入檔是彎引號）是同一個字元問題的兩面**：
**一面是掛名在池中裂成兩個字串、一面是 key 的集合比對對不上。**
**→ 盲區清單現在有七種**（第 611 條原五種 ＋ 第 1672 條的跨文字系統 ＋ 本條的標點不同形）。
**→ 後批通則：掛名與盤名裡的 `'`、`’`、`-`、`‐`、`–` 一律先正規化再比對，取 ASCII 形。**

## 第 1703 條（主線）：**團名形的分界收斂成一句——「池中有沒有同系列前作」**

策展 b 在同一組裡給出正反兩例，**這是本線第一次有對照組**：
- ✅ **`Wayne Shorter Quartet` 建團名形**——**池中十三張都是別的編制**。
- ❌ **`Robert Glasper Experiment` 不建、判裸名 `Robert Glasper`**
  ——**池中 seed 已有《Black Radio》，而且 `seed_cards.json` 雲端不能改。**

**→ 第 307 條在團名形上的具體判準**：**先看池中有沒有同系列前作；有就沿用那個字串，沒有才建團名形。**
⚠ **「三邊用團名形」只是必要非充分條件**（c-160 b 第 1665 條已記過）——**本條補上了充分那一半。**

## 第 1704 條（主線）：**(甲) 的第三種形狀——先前只有影像載體發行過的錄音**

**Monk《Paris 1969》先前只有日版 Pioneer 雷射影碟與法國盜版 DVD**
——**首次聲音發行仍判 (甲)**。
⚠ **判準要分清**：**(己) 擋的是「本碟自己的載體只有影像」，不是「先前的發行是影像」。**
**→ 收藏線判準六句的 (甲) 現在有六種認法**（五種 ＋ 本條）。

## 第 1705-A 條（主線）：**三條查法上的省時訣竅**

1. **MB `label-info` 同一筆並列「老號段＋日版號段」（例 `LT-993`＋`TOCJ-50277`）＝ (乙) 直接成立**
   ——**不必再查 Discogs。**
2. **盤名像系列名時先看 Discogs 的 `series` 欄**（頂層欄位，**比 `format` 早亮**）
   ——Duke Ellington 那張逐字 `Jazz Inspiration (2)`、全庫同系列 68 筆。
3. ⚠ **紙本 2013 的檔名有三種形**（`Billboard-`／`Billboard-BB-`／**`Billboard--` 連續兩連字號**）
   ——**枚舉週六只拿得到 48／50，一定要走索引頁 `Billboard-Magazine.htm`。**
   **這與第 1691 條是同一條**：**2010 以後一律走索引頁。**

## 第 1706 條（主線）：**MB frd 又錯一次，這次是三個半月**

**Glasper《Black Radio 2》的 MB frd `2013-05-13` 取到的是日版誤建日期，真街頭日是 Billboard 逐字的 `Oct. 29`。**
⚠ **本線的 frd 錯誤已經有四種成因**：取到日版（本例與 Cassandra Wilson）、取到宣傳盤（Van Hunt 差十年）、
取到巴西版（Norah Jones）、把日方月日配美方年份（Ron Carter）。
**→ `year` 依據序裡 MB frd 排最後，這一條已經被實證四次，不必再懷疑。**
