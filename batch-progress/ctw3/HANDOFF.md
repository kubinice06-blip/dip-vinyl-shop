# c-TW 留置 26 張：封面重測結果（2026-08-31）

**結論：2 張可解、24 張確定要掃圖。** 兩條路都走到底了。

## 一、我先更正自己的判斷

我看到抽驗有兩張命中，說「值得整批 26 張重測」。整批跑完的結果是
**命中就是那兩張**——重測沒有多找回任何一張。Apple 台灣的目錄確實有變動，
但變動的幅度不足以改變這批的處境。

## 二、Apple 台灣（`covers-probe.json`）

每張都試了四種掛名寫法：原名、原名＋樂團、原名＋合唱團、去掉「樂團」。
標題要真的對得上（完全相同或長度差 ≤8 的包含關係），年份差三年以上一律不採
——那是再版或同名作。

| 卡 | 結果 |
|---|---|
| 骨肉皮《壹玖玖伍 台灣地下音樂檔案〈肆〉》 | ✅ `壹玖玖伍台灣地下音樂檔案 <肆>` / 骨肉皮 / 1995 |
| 刺客《I Don't Care》 | ✅ `I Don't Care` / **刺客樂團** / 1994 |
| 其餘 24 張 | ✗ |

刺客那張 2026-08-23 落空的原因就是掛名：Apple 上是「刺客樂團」。
**這個教訓要留著**——華語圈的團名在 Apple 上常帶「樂團」「合唱團」後綴，
單試一種寫法會誤判成「目錄裡沒有」。

## 三、MusicBrainz → Cover Art Archive（`caa-probe.json`）

8/23 的紀錄寫「CAA 覆蓋率是零，44 個端點逐一試過全 404」。這次重跑完整鏈路：
先向 MB 查 release-group，再對 release-group **與其底下每一個 release** 試 CAA
——封面常常只掛在某一個 release 上、group 層是空的，只查 group 會漏。

```
MB 有建檔 6／24｜CAA 有圖 0
```

**CAA 是確定的零**，不是查法問題。但這一輪的副產品有用：

| 卡 | 新查到的 rgMbid |
|---|---|
| 濁水溪公社《裝潢》 | `7a04b96a…` |
| 賽璐璐《春天的天氣》 | `e945c06b…` |
| 亂彈《希望》 | `567fa0ab…` |
| 好客樂隊《好客戲》 | `6a016064…` |
| 表兒《熱血男兒硬起來》 | `e837d203…` |
| 阿飛西雅《提去買藥仔》 | `e2daf9df…` |

這 6 張的身分可以從人工舉證改成 **MBID 釘選**（§1 硬規則的正解路徑），
完整值在 `caa-probe.json`。其餘 18 張 MB 也查無，維持人工身分。

## 四、本機要做的

1. **2 張**：用 `covers-probe.json` 的 `coverUrl`（1200×1200）與 `collectionId` 直接補封面與試聽，即可上架。
2. **6 張**：把 `caa-probe.json` 的 `rgMbid` 寫回身分欄，改成 MBID 釘選；封面仍需掃圖。
3. **24 張**：等掃圖。研究、三軸、簡介 8/23 全部做完了，**不需要重跑**。

## 五、雲端不必再試的

- Apple 台灣：四種掛名寫法 × 26 張全試過。
- Cover Art Archive：release-group 與每一個 release 都試過，零命中。
- Bandcamp、Discogs 圖床、台灣各站台：本環境的 egress proxy 擋死，不是查法問題。
