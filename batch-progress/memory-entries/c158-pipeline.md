### 2026-09-18｜dip-vinyl-shop｜c-158（Blue Note 2001–2004）五層管線走完，37 張

**改動摘要**：切片 45 張走完五層，**收 37 退 8（退貨率 17.8%）**。身分 37/37 pinned、
簡介 37/37 全 full（211–239 字）、封面 29/37（替代來源 8/8 查實且目視核過版式）、
串流 32/37（5 張走固定無來源狀態）。本批 apex 0 張、無合輯、例外欄全空。

**主要檔案**：`desc-tools/batches/{cards,research,hooks,input,output}/c158-*.json`、
`batch-progress/c158/{prop-a,prop-b,slice,caa,apple-candidates,rulings,HANDOFF}`、
`batch-progress/probe/previews.json`。

**驗證結果**：`chk-prop` 四道全過、`qa-batch research/hooks/out c158` 全清、
`chk-hook-crossgroup c158` 37 張跨組全過、`fix-spacing` 兩檔各跑一次待補 0。

**三條值得記住的**：
1. **第 1560-Y 條**——**欄位殘留這次跑進 `facts` 欄了，而且帶著合法的 `src`**
   （Harris 的 Segal 獎、PJJ 的錄音室資歷名單）。**一條 `facts` 裡前半真、末句錯，下游照規矩寫照樣出錯。**
   **`src` 保證的是「有出處」，不保證「每個子句都成立」。** 已就地標註而非刪除（facts 是本機查核的底本）。
2. **第 1560-AB／AK 條**——**`facts` 標題的數字與它自己的清單對不上**，本批兩例（六首/五首、四首/三首），
   `qa-batch` 不會亮燈。**新通則：facts 出現數字時一律與同條清單對數。**
   ⚠ 兩例出自同一位研究代理，**建議本機對 `c158-b.json` 的數字再掃一遍**。
3. **第 1560-C／G 條**——**探測層的店面清單漏了北歐**（Perko《Kaanaanmaa》只有 fi 有軌），
   而且更深一層的原因是**芬蘭線的碟在非芬蘭市場是以英文盤名發行的**
   （《Kaanaanmaa》＝《Land of Canaan – Northern Hymns》，目錄號也換了）。
   **後批通則：北歐掛名的碟，店面與紙本都要多試一組英文盤名。**
