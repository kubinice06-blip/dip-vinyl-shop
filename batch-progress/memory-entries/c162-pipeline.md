### 2026-09-19｜dip-vinyl-shop｜c-162（Blue Note 2010–2013）五層管線走完，36 張

**改動摘要**：切片 45 張走完五層，**收 36 退 9**（a 18、b 18）。
身分 36/36 pinned、**簡介 36 張全 full 且字數 227–240**、封面 32/36（研究層撿到其中 3 張可用網址）、
串流 34/36（**其中 5 張是人工回收**，2 張走固定無來源狀態）。本批 apex 0 張、無合輯、例外欄全空。

**主要檔案**：`desc-tools/batches/{cards,research,hooks,input,output}/c162-*.json`、
`batch-progress/c162/{prop-a,prop-b,slice,caa,apple-candidates,rulings,rulings-mainline,HANDOFF}`、
`batch-progress/probe/previews.json`。

**驗證結果**：`chk-prop` 標記 0、`qa-batch research/hooks` 全清、`out` 一筆已載明的良性誤報
（`Gov't Mule` 的 `未具名出處?` 命中 hook 原文，正文下一句就具名）、`chk-hook-crossgroup` 36 張全過、
`qa-check-research` 兩檔標記 0 且 **hook 原封開頭 36/36**、`fix-spacing` 兩檔待補 0。

**五條值得記住的**：
1. **探測層 `tried` 的 `N→0` 與 `0→0` 是兩種失敗**（第 1741-B 條）。`N→0` ＝找到候選但 `titleOk` 濾掉，
   **碟在架上、只是副標差異，幾乎一定找得到**；`0→0` 才可能是真的沒有。**這條決定要不要花工時。**
2. **幽靈條目會出現在單一市場**：`Duende` 的同一個 Apple id 在 `us` 回 0 軌，**13 個其他市場全回 10 軌且都有預覽**。
   **用 id lookup 時單一市場回 0 軌不代表沒有預覽，要換市場。**
3. **第 1739-B 條**——⚠ **`note` 的預算要算「字元」不是算「項目條數」**。
   鉤子層回報「18 張全落在 214–240」，寫作層實測初稿 18 張全部超過 240（中位數 +45），13 張被迫整格捨去。
   **根因是沒算拉丁專名的字元成本**（`Gerald Wilson Orchestra` 21 字、`Darek Oleszkiewicz` 18 字）。
4. **第 1733-B 條**——**一條 `fact` 的中文斷語不得超出它自己引的原文**。
   抓到一條：官網逐字只有「這對夫妻 2010 年發了這張碟」，`f` 卻寫成「兩人第一張聯名作品」。
   ⚠ **這一類 `qa-batch` 比對不了（需讀懂語意），只能靠下游代理逐條讀 facts 時發現。**
5. ⚠ ⚠ **派工信連四次寫錯規則或斷言資料**（第 1725／1729／1734-B／1738-B 條），
   **根因都是「憑記憶複述正本」或「憑印象斷言資料」**。已立體例：
   **派工信第一段指定讀 base 檔並聲明以它為準；不得出現任何「怎麼下筆」的細則；不得斷言池中有什麼，只給掃描清單。**
   **七支代理照 base 檔走並回報衝突，七次都是對的。**
