### 2026-09-25｜dip-vinyl-shop｜c-187（日本爵士獨立廠牌線 jp-2 第五批）五層管線走完，28 張

**改動摘要**：1979–1981 年段，**38 張收 28 退 10（74%，本線收件率最高的一批）**，
身分 **28/28 pinned、零 §1 人工**、簡介 **28/28 全 full（204–239）**、
**封面 24/28、串流 21/28**（其中 1 張是人工回撈救回的）。apex 0 張、例外欄全空。

**主要檔案**：`batch-progress/c187/{slice,prop-a,prop-b,caa,apple-candidates,rulings,HANDOFF}`、
`desc-tools/batches/{cards,research,hooks,input,output}/c187-*.json`、
`batch-progress/chk-dispatch.mjs`（第九道）、`desc-tools/prompts/hook-base.md`、
`batch-progress/c163/rulings-mainline.md`（第 1986-B–1989-B 條）。

**驗證結果**：`chk-prop` 兩組 0、`dedup-crossbatch` 四道 0、三階段 `qa-batch` 全過、
`chk-hook-crossgroup` 28 張全過、`qa-check-research` 兩組 0、`fix-spacing` 兩檔 0；
**鉤子預算 216–230 與 desc 204–239 都是我自己逐筆重算的**；
跨兩組 28 張的 4-gram 我自己重掃，**漢字與片假名 ≥3 張都是 0 條**（片假名連 2 張的都 0 條）；
**首句照抄 hook 28/28、`key` 逐字同序。**

**五條值得記住的**：
1. ⚠ ⚠ ⚠ **派工信要求正文寫一個研究稿裡根本沒有的年份**（本線第一次）：
   同批跨組的硬約束指定 a 組寫「1981 年再錄一次」，而 `1981` 在 a 組整份研究稿裡零次
   ——**鉤子層用程式驗出來並改寫成「後來被重錄過一次」，事實不減、兩張仍然對得上。**
   **→ 配跨組約束時，要求寫的每個數字都先回研究稿查一次。**
2. ⚠ ⚠ **自檢工具要比對「repo 裡實際存在的東西」，不要比對「我想像中那個東西長什麼樣」**（第 1988-B 條）：
   派工信自檢第九道的第一版用字元類猜「掛名長什麼樣」，回測八個假警報
   ——**改成比對全 repo 卡單湊出來的掛名詞表，零假警報。**
   ⚠ **這個錯法與第 1959-B 條（用白名單列舉日本市町村）同形，兩次都是想用 regex 描述開放集合。**
3. ⚠ ⚠ **驗「日」後那一個字的尺本身會假警報**：`/[0-9] 日(.)/` 命中價格字串「`NNNN 日圓`」，
   **一批多報 5 處**——**改成 `/[年月]\s*\d{1,2}\s*日(.)/`。**
   **「尺本身有假警報」這種形狀最危險：代理會為了消掉假警報去改沒有問題的句子。**
4. ⚠ ⚠ **鉤子層把 28 張裡 20 張的 hook 寫成具名或物件領句，是「第二句該寫什麼」那個撞法的結構性解**
   （第 6473 條）：**hook 不用代稱，寫作層就沒有「替代稱具名」這件事可做，也就撞不起來。**
   ⚠ **而改同構時不要把三張推向同一個名詞**——第一輪把三張都改向「作曲欄」，第二輪就在那個名詞的鄰居上長出兩條新的。
5. ⚠ ⚠ **「CBS/Sony 編號前兩位是定價碼」這個機制本批有 6 個實例，只寫了 1 張**：
   **同一個機制的多個實例不能分寫在多張卡上（必撞），要整條給最能承載它的那一張，其餘各張改寫「這一張與那家廠牌的哪一個具體接點」。**
   **剩下的五個實例登記給後續批次取用。**
