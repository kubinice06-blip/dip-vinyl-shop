## 2026-09-04 — dip-vinyl-shop — c-87 走完雲端段（§1 人工身分補遺批：日本自主爵士小廠，32 張）

- **改動摘要**：全專案第一個成規模的 §1 批次，**32 張、30 位藝人**
  （a：Johnny's Disk 9 ＋ Aketa's Disk 7；b：Nadja 8 ＋ Union Jazz 8），
  **全部 `identitySource: manual`、`rgMbid` 留空**。這批補的是 c-67 收完後
  掛在 EXPANSION-PLAN「待店主決定」那一節的碟——它們在 MusicBrainz 上名下
  0 個 release-group。店主 2026-09-04 核可 §1 路線。
- **主要檔案**：`batch-progress/c87/`（prop、apple-art.mjs／json、apple-previews.mjs、
  previews.json、HANDOFF.md）、`desc-tools/batches/cards/c87-cards.json`、
  `research/c87-{a,b}.json`、`hooks/c87-hooks-{a,b}.json`、`output/c87-out-{1,2}.json`。
- **驗證結果**：`qa-batch research/hooks/out c87` 全清；`qa-check-research` 兩檔各 0 標記；
  `fix-spacing` 待補 0；`chk-hook-crossgroup` 32 張、hook 加權 14.5–36.5、note 320–350；
  out-1 209–235、out-2 217–235、`>260` 為 0；`chk-prop` 標記 0、跨批撞卡 0；
  **主線獨立複跑 `checkManualIdentity` 32/32 通過**。
  **封面 17/32、試聽 17/32。**
- **管線與一般批不同**：`fix-rgmbid` 與 `probe-caa-generic` **都不跑**
  （前者會改壞人工身分、後者以 rgMbid 為鍵）；封面走 §4 的
  `apple-verified-collection`，用策展層人工核對過的確切 collectionId 直查。
- **這批的裁定與教訓**：
  1. **立第 134 條**：§4 那個人工核對過的 collectionId，封面能用，試聽也能用。
     六張在 Apple 上實際存在但盤名對不上（加曲版的「+3」「+2」、片假名盤名、
     整個換掉的盤名），search 找得到、標題比對擋掉——**而擋掉是對的**，
     因為 search 沒辦法確認找到的是不是同一張碟。當來源本身已經人工確定，
     比對擋掉的就是正確答案。試聽 10→15。
  2. **立第 137 條**：`itunes.apple.com/lookup` 不帶 `country` 就是問美國店面。
     日本限定的碟會回「**專輯有、曲目一筆都沒有**」——那不是「有碟無預覽」，
     是問錯了店面。這個假象比第 122 條那幾種更難看穿，因為它回的是**正確的那張碟**、
     只是少了你要的那一半。試聽 16→17。**附錄把這條路徑推廣到一般批次**
     （新增 `apple-previews-verified.mjs`，c-76 立刻用上一張）。
  3. **研究層推翻策展層 35 處，比 c-72 的 20 處還多。**
     §1 的碟沒有維基條目，策展層只能靠 Discogs 條目與少數日文記事，
     **推論的比例天生就高**。最大的一處是身分：与世山澄子的「第一張」
     被策展層寫成「第二張、隔十三年」——它引為前作的《INTERLUDE》，
     其實是她 1972 年在那霸開的**爵士店店名**。
     另有一處**方向反了**：策展層特地警告「碟上的 John Lewis 不是 MJQ 的那位」，
     實際上就是。**策展層的警告本身也要查證。**
  4. **第 135 條第一次出現「歸納經查證成立」的案例**：b 組寫作層回八首逐首核過
     柳原《Summer Rain》的「最短 3:27、最長 6:12」，成立、照用；
     同一支代理核出藤井那張的「八到十一分鐘」不成立，改逐首寫原始值。
     **回去數不是為了推翻，是為了確認。**
  5. **§1 薄資料的結構性同調**：多數碟除了 credit 與曲目表沒有別的材料，
     hook 層有六條 hook 落在 credit 欄位；寫作層在 desc 第二拍再岔開一次。
     b 組寫作層第 131 條做了兩輪——第一輪發現 16 張裡有 8 張以
     「某某 19XX 年生於某地」作結、其中四張連句型都一樣。
  6. **策展層踩到第 122 條「MB 回了完全無關的東西」十三次**（元岡一英→岡部啓一、
     大森明→中森明菜、リー・ウォンヒー→Jerry Lee Lewis、与世山澄子→川澄綾子），
     逐一 browse 排除。**§1 批次的舉證負擔主要就花在這裡。**
- **這條線還有多少**：沖繩線另有十位確認實體存在、名下 1960–95 年 0 個 RG 的藝人；
  美國福音線的 Nashboro 一家在 Discogs 有 260 張 1955–81 LP、MB 只建 4 筆。
  **§1 這條路一旦開通，它能補的洞比原本估的大得多。**
- **下一步**：本機端上傳（三軸、頂點資格、四處寫入與回讀、15 張封面掃圖）。
