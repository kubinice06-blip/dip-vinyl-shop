### 2026-09-19｜dip-vinyl-shop｜c-169（Blue Note 2000–2019，列舉漏切補批之一）五層管線走完，40 張

**改動摘要**：切片 41 張走完五層，**收 40 退 1**（退的是 EP；a 20／b 20 兩組零退件）。
身分 40/40 pinned、簡介 40/40 全 full（193–238）、**封面 25/40、串流 38/40**（3 張回撈救回，2 張是單張版權缺口）。
本批 apex 0 張、無合輯、例外欄全空。

**主要檔案**：`desc-tools/batches/{cards,research,hooks,input,output}/c169-*.json`、
`batch-progress/c169/{prop-a,prop-b,slice,caa,apple-candidates,rulings,HANDOFF}`、`batch-progress/probe/previews.json`。

**驗證結果**：`chk-prop` 兩組標記 0、`qa-batch research/hooks/out c169` 全清、
`chk-hook-crossgroup c169` 40 張全過、`qa-check-research` 兩組標記 0、`fix-spacing` 兩檔待補 0、
**首句與 hook 逐字相符 40/40**；六條硬限制與研究層十七處推翻全部實掃 0 命中。

**三條值得記住的**：
1. ⚠ ⚠ **我把「一次觀察」升格成「規則」，而且是 base 明文禁止的那一種**（第 1838-B 條）。
   我在寫作層派工信裡寫「鉤子層砍乾淨後**必然高估**、不要預留上浮」——
   **`writer-base.md` 明文禁止從卡的組成預測偏差方向**，而 **a 組實測是初稿 5 張超標、0 張撞下限**，正好相反。
   **派工信從此只留 base 原文那半句：「寫完先量，兩端都要驗」。**
   ⚠ **本輪這類錯（把說明、括號、單次觀察當成條件）我一共犯了四次**：第 1795-B／1830-B／1837-B／1838-B 條。
2. **`sound` 欄是寫作層唯一的緩衝格，兩個方向都能調**（第 3442／3372 條）：
   太長就整格捨去 `sound`、太短就補回 `sound`，**一次都不要從 `facts` 撈鉤子層已砍掉的格**。
   兩組合計捨 7 格、補 3 格，**全部只動 `sound`**。
3. ⚠ **「歐洲分支覆蓋率低」要拆成兩半**：**串流 38/40 與現役目錄同級（95%），差的是封面（CAA 25/40）**。
   **CAA 的 release-group 層對非美國本部的發行覆蓋明顯偏低，Apple 則幾乎一視同仁。**
   同一條線上：**`bluenote.com` 有沒有專頁的分界也是出身地不是年代**（本批美國本部 11 張有 9 張有頁、分支 9 張 0 頁）。
