# c-172 交接（2026-09-19）：藍調之音 1985 後線的**最後 3 張**，全線收尾

**這批可以接本機上傳了。** 雲端能做的全部做完，剩下的是雲端依 `REMOTE_RUNBOOK.md`
**不能做**的（KV、Firestore、`seed_cards.json`、`album_overrides`），不是沒做完。

⚠ **這是 c-148…c-172 整條線的收尾批。全線 839 張，到此全數走完雲端段。**

## 一、這批是什麼——三張各自代表一種「列舉／查詢會漏掉」的失效形狀

三張都**不在 `enum/blue-note.json` 的可用列裡**，而且**成因各不相同**：

| 卡 | 失效形狀 | 逐字證據 |
|---|---|---|
| `Kevin Eubanks《Spirit Talk》` | **第 5 種：`label-info` 掛成母公司** | 唯一那筆 release `fc09d997` 的 `label-info` 逐字 `[('Capitol Records', 'CDP 0777 7 89286 2 1')]`——**廠牌軸列舉永遠碰不到**（第 2487 條型）。⚠ Billboard 榜單逐字把廠牌印成 `BLUE NOTE 89286/CAPITOL`，Discogs 那一端 `labels` 只有一格逐字 `Blue Note`。 |
| `Stefano Di Battista Quintet《A prima vista》` | **第 5 種（同形）** | release `1eaa4e09` 的 `label-info` 逐字 `EMI Music France`／`7243 4 97945 2 8`，不是 Blue Note。**與同藝人 c-171 b《Round About Roma》一模一樣。** |
| `Stefano Di Battista《Parker's Mood》` | **第 7 種：列舉列的 `year` 是 `null`** | MB 端 `label-info` 這次掛對了（release `3c5ef34e` 逐字 `[('Blue Note', '8661712')]`），**是年份排序切片把它甩出去的**。 |

**3 張收 3 退 0**，年份改判 0。

## 二、⚠ 作廢一條我自己下過的判斷，與一條查詢語法的更正

1. **「第八種形狀＝MB 根本沒有這張」不存在**（第 1844-B 條）。
   我曾判《Spirit Talk》查無、要走 §1 人工。**真正的成因是 `title:` 不是 release-group／release
   搜尋索引上的欄位**——MB 的 Lucene **不報錯、靜靜回 `count 0`**。
   改用 `releasegroup:` 回 `count 1`、`score 100`，**當場釘住，零人工身分。**
   ⚠ **不可推廣成「改用 browse」**（browse 要先有藝人 MBID，取代不了搜尋），
   **也不同於 Apple `/search` 的問題**（那是市場覆蓋，這是查詢語法）。
2. ⚠ **主線待辦**：`c-104` 以後所有判過「MB 查無」的裁定，**若當時用的是 `title:`，結論都不可信，要重掃。**

## 三、逐項對照 `ALBUM_ONBOARDING.md` 的完成標準

| 項目 | 狀態 |
|---|---|
| 身分（rgMbid 釘住） | **3/3 全部 pinned，零 §1 人工** |
| 三軸與頂點資格 | 卡單已帶；**本批 apex 0 張** |
| 固定簡介（desc） | **3/3 全 full**，字數 **232／239／232** |
| 封面 | **2/3**（缺 `Kevin Eubanks《Spirit Talk》`，CAA `art` 逐字 `null`） |
| 固定試聽／無來源狀態 | **3/3 全部有串流** |
| §5.5／§5.6 例外欄位 | 例外欄全空 |
| published gate | 本機端 |

⚠ **本線唯一一次探測層零 unavailable、完全不需人工回撈。**
（對照：c-166…c-170 探測層共標 31 張 unavailable，人工回撈證實 **20 張（65%）是誤判**。）

## 四、⚠ 本機審稿要盯的四件事

1. ⚠ **策展層 `why` 有一句是錯的**：「《Parker's Mood》十軌全是 Bird 的原創」——**不成立**。
   逐軌 `Written-By` 實查：**十軌無一出自 Di Battista，但真正出自 Parker 的只有四首**。
   正文已按實查寫。**研究層曾把這句與派工信的「十軌全是別人的曲目」併稱兩句都不準，那是研究層誤判**——
   後者是對的（第 3683 條）。
2. ⚠ **四處刻意整格不寫**（全批實掃 0 命中）：
   `Parker's Mood` **不寫美版 2005 與任何發行時點**（寫了就給正文一個抽屜去翻出美版）、
   **不得寫成「Parker 作品集」**（主故事鏈首格即 2004 年米蘭新錄音）；
   `Spirit Talk` **不寫 1993 年 4 月發行日、不寫曲風**（全批 0 處曲風字樣）。
   ⚠ 這四條**全部用「整格捨去」達成，note 與正文 0 處否定句**——
   不要看到缺了就補回去。
3. ⚠ **兩張 Di Battista 的「誰寫了這些曲子」是互為鏡像的軸，已裁定歸《Parker's Mood》獨佔**；
   《A prima vista》整格讓出，**連〈Lush Life〉借曲也不寫**。這是刻意的，不是漏。
4. ⚠ **研究層 `note` 有一處與 `writer-base.md` 直接牴觸**：`Spirit Talk` 的 note 逐字引 Cash Box
   「**全片**有一種易於進入的空靈感」，而 base 明文禁用「全片」指整張專輯。
   **寫作層已以 base 為準改寫成「它」，語意零損失**（第 3697 條）。
   下次再引這段樂評，**`note` 那一格要先把「全片」換掉。**

## 五、五層逐層數字

| 層 | 結果 |
|---|---|
| 策展 | 3 收 0 退，`chk-prop` 標記 0，`dedup-crossbatch` 撞卡 0 |
| 探測 | 串流 **3/3**、封面 **2/3**、未 ready **0 張**（本線唯一） |
| 研究 | **3/3 全 full**，36/36 https 來源，`qa-batch research` 全部通過 |
| 鉤子 | 預算 **223／228／229**（上限 230），hook 加權 24–26、note 218–232；兩支腳本全過、旗標 0 |
| 寫作 | desc **232／239／232**，`>240` 0、`<180` 0；三支腳本全過 |

⚠ **寫作層本批單向低估 8–13 字元（初稿 248／253／232），`sound` 一格都沒動用**——
note 鏈全是主故事所需、無格可捨，收斂靠砍本層自己加的連接子句。
**方向不可繼承**：c-169 a 組是反向（5 張寫不夠長）。**下一批仍要兩端都量。**
