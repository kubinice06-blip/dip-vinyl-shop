# c-157 交接（2026-09-18）：Blue Note 2000–2002，40 張走完雲端段

**這批可以接本機上傳了。** 雲端能做的全部做完，剩下的是雲端**依 `REMOTE_RUNBOOK.md`
不能做**的（KV、Firestore、`seed_cards.json`、`album_overrides`），不是沒做完。

## 一、這批是什麼

切片 45 張，**收 40 退 5**（退貨率 11.1%）。2000–2002 這一段 Blue Note 的自家新錄音仍佔多數，
**收藏線判準六句（甲～己）用到的次數不多。**

## 二、逐項對照 `ALBUM_ONBOARDING.md` 的完成標準

| 項目 | 狀態 |
|---|---|
| 身分（rgMbid 釘住） | **40/40 全部 pinned，零 §1 人工** |
| 三軸與頂點資格 | 卡單已帶，本機組 manifest 時照跑；**本批 apex 0 張** |
| 固定簡介（desc） | **40/40 全 full**，out-1 210–239／out-2 204–237，**兩端都沒撞到** |
| 封面 | **28/40**（**缺 12，替代來源 12/12 全部查實**） |
| 固定試聽／無來源狀態 | **37/40 有來源**，**3 張走固定無來源狀態** |
| §5.5／§5.6 例外欄位 | 本批無合輯，例外欄全空 |
| published gate | 本機端 |

**3 張走固定無來源狀態**：Ron Carter《When Skies Are Grey》／Stan Getz & Albert Dailey《Poetry》／
Antti Sarpila & Severi Pyysalo《New Moods - New Sounds》。**三張的正文都零處提串流或試聽。**

## 三、⚠ 這一批最重要的一條（第 1499／1503 條）：裁定只清 `note` 是不夠的

**主線下「某軌名／某年份不得寫」的裁定時，只寫進 `rulings.md`、鉤子層只清 `note`
——研究稿的 `sound`／`keyTracks`／`hookCandidates` 三欄沒有人負責清。**
**本批兩組合計抓到八處殘留，全部由寫作層在動筆前比對發現。**

| 組 | 處數 | 最危險的形狀 |
|---|---:|---|
| a | 2 | `sound` 欄帶著 `facts` 裡沒有 src 的年份（1983） |
| b | 6 | ⚠ **兩處是「某條線全批只准 1 張當主軸」的反同構條款，被 `sound` 欄繞過去**——而且那條線在 `facts` 裡**有 src**，下游沒有理由懷疑它 |

⚠ **下游若有任何工具直接吃這三欄（本機組 manifest 時很可能會），裁定就等於沒下過，
而且沒有任何檢查會亮燈。**

**→ 主線已逐筆改 `c157-{a,b}.json`（清欄位、加 `_mainlineNote` 記明原因）。**
**→ 建議本機加一道檢查**：把 `rulings.md` 裡所有「不得寫」的字串與研究稿四個文字欄位對撞。

## 四、⚠ 字數的偏差方向不可繼承（第 1504-B 條，推翻了我自己的第 1500 條）

| 組 | 初稿撞 240 | 初稿掉 180 | 終稿 |
|---|---:|---:|---|
| writer-1（a，18 張） | **9** | 0 | 210–239 |
| writer-2（b，22 張） | **0** | **5**（最低 158） | 204–237 |

**writer-1 事前已按 +25 上浮仍有一半撞破 240（實測低估中位 +35、最大 +80）；
主線據此叫 writer-2 按 +40～+80 上浮，結果 writer-2 反而五張掉到下限以下。**

**→ 這正是 `writer-base.md` 第 107–125 行早就記過的「同一批兩組方向相反」（w2-118）。**
**真正可繼承的只有「算完預算表要同時對照 240 與 180 兩端」，
連「依同批另一組的實測方向判斷」都不可繼承。**
**主線自律：不要再把某一組的偏差方向寫進下一組的派工詞。**

⚠ **但有一個形狀是可靠的**（c-158 再度驗證）：**低估最嚴重的是「單張卡要點名五、六個拉丁專名」
或「兩間以上錄音室」的卡**——看的是單張卡的專名密度，不是批次屬什麼曲風。

## 五、⚠ 派工詞的四類系統性錯誤（第 1494／1496 條）

本批主線寫錯 **11 處**，代理全部接住並駁回。四類：

1. **指定的軸在 `facts` 裡沒有 src（7 處）**——其中**五條是真的查到了、只是研究層放進了 `notes`／`hookCandidates`**。
   **純粹的格式損失，不是查證不足。** `qa-batch research` 不會亮燈（它檢查 `facts` 的格式，不檢查 `notes` 裡有沒有埋東西）。
2. **把 a 組的東西派給 b 組（3 處）**——成因是主線讀自己的**跨組合計**驗收裁定去寫**單組**派工詞。
   **→ 派工詞給某一組的數字要先按組拆開；跨組合計非寫不可時要明寫「這是全批合計，你這組只能證到其中 N 項」。**
3. **把裁定編號派錯組（第 1436 條，c-156 已發生過一次）。**
4. **把研究稿 `notes` 的形容詞當成事實抄進派工詞。**

## 六、本機接手時要注意的

1. **通論帳本讀不到**（`desc-restyle/progress.json` 不在版控內）——雲端只做到「批內不重複」，
   **跨批次的通論重複請在逐張審稿時把關**。
2. **`collectionId` 全卡池唯一性檢查**（c-156 第 1433 條的建議，本批同樣適用）。
3. **12 張封面走替代來源**，⚠ **《My Lullaby》最特別：2002 Not Two 原盤的封面在 Discogs／Apple 兩層都查不到，
   只有 2007／2011 再發版藝術**（CAA 那一層 b 組研究稿沒有記錄）。
4. **4 張走 Apple `600x600bb`**，⚠ **《Melange》的 600px 那筆是白色宣傳紙套、《On Tour》只有 225px。**
5. **`qa-batch out` 在雲端段跑得起來**（第 1507-B 條撤銷了先前的第 1501 條）；
   ⚠ **`qa-batch hooks` 提示「略過 qa-check-hooks.mjs：本 repo 無此檔」那一條仍是真的缺口**，
   字數／禁語／開頭雷同／分數星等改由 `chk-hook-crossgroup.mjs` 擋。

## 七、產物清單

```
desc-tools/batches/cards/c157-cards.json          40 張卡單
desc-tools/batches/research/c157-{a,b}.json       研究層（主線已清過殘留欄位）
desc-tools/batches/hooks/c157-hooks-{a,b}.json    鉤子層
desc-tools/batches/input/c157-writer-{1,2}.json   merge 產物（18／22）
desc-tools/batches/output/c157-out-{1,2}.json     寫作層（18／22）
batch-progress/c157/{prop-a,prop-b,slice,caa,apple-candidates}.json/md
batch-progress/c157/rulings.md                    第 1440–1507-B 條
batch-progress/memory-entries/c157-pipeline.md    備忘錄條目（本機貼進 PROJECT_MEMORY.md）
```
