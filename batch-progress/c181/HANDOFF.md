# c-181 交接（2026-09-22）：jp-1 線第九批，1985–1987，28 張

**這批可以接本機上傳了。** 雲端能做的全部做完，剩下的是雲端依 `REMOTE_RUNBOOK.md`
**不能做**的（KV、Firestore、`seed_cards.json`、`album_overrides`），不是沒做完。

## 一、37 張提案收 28 退 9（76%）

## 二、逐項對照 `ALBUM_ONBOARDING.md` 的完成標準

| 項目 | 狀態 |
|---|---|
| 身分（rgMbid 釘住） | **28/28 全部 pinned，零 §1 人工** |
| 三軸與頂點資格 | 卡單已帶；**本批 apex 0 張** |
| 固定簡介（desc） | **28/28 全 full**，字數 **214–239** |
| 封面 | **19/28** |
| 固定試聽／無來源狀態 | **23/28**，**5 張走固定無來源狀態** |
| §5.5／§5.6 例外欄位 | 例外欄全空 |
| published gate | 本機端 |

**5 張無來源**：`Some Years Parst`／`Super Friends`／`エイリアン魔獣鏡`／`Dear Friends`／`More Than You Know`。

## 三、⚠ 本機審稿要盯的六件事

1. ⚠ ⚠ **`高中正義《Jungle Jane》` 與 `《Jungle Jane Tour Live》` 的來源曾經雙雙配錯**
   （兩張同時被配到《Takanaka》1977），**探測層改用日本店面優先之後兩張各自配對正確**
   （jp 上逐字的 `JUNGLE JANE` 1986 與 `JUNGLE JANE TOUR LIVE` 1986，主線第 1946-B 條）。
2. ⚠ ⚠ **`MALTA` 兩張 LP 的 Discogs credits 被 1983 年首作的名單整塊覆蓋**
   （`Eric Gale` 從 `Electric Guitar` 被抄成 `Electric Bass` 就是指紋，主線第 1951-B 條）
   ——**本批正文的編制照研究稿寫，不是照 Discogs 的 LP 頁。污染名單五個名字在兩張卡上 0 命中。**
3. ⚠ **`qa-batch` 對本批留了兩筆「未具名出處?」標記，我逐筆看過判定放行**：
   「《Swing Journal》讀者票選的鼓手第一」與「第一屆日本ゴールドディスク大賞 的年度專輯獎」
   **主辦者與頒發單位都已在句子裡。** 那一道是刻意不做豁免、只印前後文交人工判的設計。
4. ⚠ **同一批另有一格是反向處置**：`中本マリ《Lady In Love》` 的「連八年讀者票選第一」**整條捨去**，
   因為沒有主辦者。**判準：主辦者跟著進正文才可寫。**
5. ⚠ **年份改判 3 筆、盤名改判 1 筆都在策展層做掉了**：
   `中本マリ《Lady In Love》` 1985→**1981**、`《Moods Of A Lady》` 1985→**1982**、
   `鈴木良雄《Touch of Rain》` 1986→**1985**；`土取利行` 取《磬石（サヌカイト） 古代石の自然律》。
   **三筆年份都是「MB 整個 RG 只建了 CD 再發」那一形，`titleCheck` 三欄一致、機器完全不報。**
6. ⚠ **`小宅珠実 With Great Jazz Quartet《Hot Flutes》` 錄於 1985-10-16，
   而前一天同一組四人錄了另一張碟**（King All Art `K26P-6438`，同公司、隔一天）
   ——**`versions` 與 dedup 都抓不到這種關係。**

## 四、驗證（都是我自己重跑複核的）

- `chk-prop` 兩組標記 0、`dedup-crossbatch c181` 跨批撞卡 0
- `qa-batch research/hooks/out c181` 三階段（out 的兩筆已判定放行）、`chk-hook-crossgroup c181` 28 張全過
- `qa-check-research` 兩組標記 0、`fix-spacing` 兩檔待補 0
- **鉤子預算 28 筆我自己逐筆重算**：226–230，與代理報的逐格相符
- **desc 28 張 214–239**，`<180` 0 張、`>240` 0 張、**首句照抄 hook 28/28**
- **跨兩組 28 張的 4-gram（漢字＋片假名混掃）我自己重掃**：≥3 張的都是廠牌名與人名，**0 條句型同構**

## 五、三件給下一線的

1. ⚠ ⚠ **鉤子層在本批抓到七處「hook 靠在壓預算時被砍掉的那一格上」**
   ——**初稿超標是常態，壓預算靠整格捨去，而 hook 常常正是靠在那一格上：
   動筆時站得住、減肥之後踩空。這一道必須排在壓完預算之後。**（已寫進 `hook-base.md`。）
2. ⚠ **合掃要一次把漢字與片假名放進同一個字元集**，不是分兩輪
   ——**混合字種的 gram 會掉在兩輪之間。**
   ⚠ **而跨越「被剝掉的拉丁字串」的 gram 是假象，要回原文讀過才算數**（主線第 1952-B 條）。
3. ⚠ **`千野秀一` 在本線第二次被寫錯**（c-178 才剛改過一次，本批卡單又寫回舊的）
   ——**同一個人名在同一條線上被改判兩次，代表更正沒有回流到列舉層的來源。**
