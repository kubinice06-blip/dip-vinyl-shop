# c-184 交接（2026-09-25）：**日本爵士獨立廠牌線 jp-2 第二批**，1972–1976，24 張

**這批可以接本機上傳了。** 雲端能做的全部做完，剩下的是雲端依 `REMOTE_RUNBOOK.md`
**不能做**的（KV、Firestore、`seed_cards.json`、`album_overrides`），不是沒做完。

## 一、38 張提案收 24 退 14（63%）

**a 組 19 張收 12 退 7**（曲風／曲目來源 3 ＋ 外國藝人 2 ＋ 撞池 2）；
**b 組 19 張收 12 退 7**（原壓廠牌不在十五家 3 ＋ 曲風 2 ＋ 撞池 1 ＋ 曲目來源 1）。

## 二、逐項對照 `ALBUM_ONBOARDING.md` 的完成標準

| 項目 | 狀態 |
|---|---|
| 身分（rgMbid 釘住） | **24/24 全部 pinned，零 §1 人工** |
| 三軸與頂點資格 | 卡單已帶；**本批 apex 0 張** |
| 固定簡介（desc） | **24/24 全 full**，字數 **208–239**（a 組 219–239／b 組 208–233） |
| 封面 | **18/24**（CAA） |
| 固定試聽／無來源狀態 | **15/24**，**9 張走固定無來源狀態** |
| §5.5／§5.6 例外欄位 | 例外欄全空 |
| published gate | 本機端 |

⚠ ⚠ **九張無來源已逐筆裁定過、救回 0 張**（主線第 1966-B 條）：
**28 筆候選全部不是本盤**——`Rumour`／`Friends`／`1999`／`Origination`／`Up to Date` 是通用英文題撞別人的碟，
**唯一掛名對得上的那筆（`LOVE WILL MAKE A BETTER YOU`）是 1971 年 5 軌的另一張碟**。
**三張連候選都沒有。下游不要再派回撈。**

## 三、⚠ 本機審稿要盯的八件事

1. ⚠ ⚠ ⚠ **十七組人名的漢字是改過的**（主線第 1968-B／1969-B 條）：
   **研究層兩組合計推翻 37 處，其中 17 組是人名，機制都是「從 Discogs 羅馬字逆推漢字選錯同音字」**
   ——**卡單（`c184-cards.json`）與 `prop-{a,b}.json` 都已由主線逐字串替換（28／18／14 處）。**
   ⚠ **兩組獨立查到同一個人的兩種錯字**（`森崎雄幸` 與 `森崎由紀夫`，兩者都是 `守崎幸夫`）。
2. ⚠ ⚠ **`本田竹広`／`本田竹曠` 兩種寫法在正文裡並存是刻意的**（主線第 1968-B 條）：
   **正文裡的樂手名照盤面的期別形（1973–74 年段是 `本田竹曠`）、掛名欄照池中先例**
   ——**不要統一。** ⚠ **`イイノホール`／`飯野ホール` 同理。**
3. ⚠ ⚠ **`渡辺貞夫` 本批 5 張（跨兩組），五條軸與五種「他」的引入法都配死了**
   （同名碟十一年／東非歌舞團同台／九人全明星與 富樫雅彦／平安夜的爵士屋／蒙特勒全日本班底）
   ——**五張的正文互不點名對方盤名。**
4. ⚠ ⚠ **三張的 `live` 是策展層改判上來的**（主線第 1958-B 條）：
   `Kenya Ya Africa`（1973-09-20 飯野ホール）／`Mbali Africa`（1974-09-20 郵便貯金ホール）／
   `In Person`（1973-09-24 郵便貯金ホール），**另 b 組 `Up-To-Date` 也是**
   ——**前兩張是「同月同日、相差一年、不同場館」，上傳時看一眼有沒有寫混。**
5. ⚠ ⚠ **`山本邦山` 兩張是三部作的第一、二張，第三張《萬華 = Mange》（`CBS/Sony SOCO 105`）不在店裡**
   ——**正文只寫到企劃名，沒有暗示三張齊全。** **補遺候選已記在 `audits/between-the-lines-candidates.md`。**
6. ⚠ **`大野俊三` 兩張年份一前一後**（`Bubbles` 1976 是策展層從 1975 改判上來的／`Something's Coming` 1975）
   ——**最容易寫反的一組，兩張的正文已互核。**
7. ⚠ **`渡辺貞夫《Swiss Air》` 的三軌 Discogs 記成 `Written-By: Traditional`**
   ——**1978 二刷的 credits 帶 `tracks` 範圍、與原壓分法相同**（主線第 1969-B 條），
   **正文沒有替那三軌補作曲者。**
8. ⚠ ⚠ **給本機的一件**：**池中 `山下洋輔トリオ《Chiasma》1976` 的 `live` 欄是錯的**
   ——**它其實是 1975-06-06 德國海德堡爵士節的實況**（Discogs notes ＋ Apple jp 盤名《Chiasma (Live)》兩個來源，
   主線第 1969-B 條）。**雲端不碰 `seed_cards.json`，請本機改。**

## 四、雲端已做完的驗證（我自己重跑的，不是照抄代理報告）

- `chk-prop` 兩組標記 0、`dedup-crossbatch c184` 四道 0。
- `qa-batch research/hooks/out c184` 三階段全過；`chk-hook-crossgroup c184` 24 張全過（否定句 0）。
- `qa-check-research` 兩組標記 0；`fix-spacing` 兩檔待補 0。
- **鉤子預算 223–230（24/24 ≤230）與 desc 208–239 都是我自己逐筆重算的**，與兩層的報告逐格相同。
- **跨兩組 24 張的 4-gram（漢字＋片假名）我自己重掃**：**≥3 張只剩 `渡辺貞夫` 一條純專名、0 條句型同構；
  片假名 ≥3 張 0 條。** 2 張殘片 12＋8 條，全是專名／樂器名／場館名，另 1 條是 hook 原文對別張正文（見下）。
- **首句照抄 hook 24/24、`key` 逐字同序同數量。**

## 五、裁定條號

`batch-progress/c184/rulings.md`：策展 5716–5745（a）／5746–5775（b）、
研究 5776–5805（a）／5806–5835（b）、鉤子 5836–5862（一人做完兩組）、
寫作 **5996–6010（a）／6011–6025（b）**。
⚠ **寫作層的條號跳號是刻意的**（主線第 1971-B 條：**新條號接全域最大值，避免不同批次撞號**；
**c-183 與 c-184 的研究層都用了 5776–5835，引用時一律寫「c-18X 第 NNNN 條」**）。
