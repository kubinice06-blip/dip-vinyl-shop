# 交接報告：卡池研究層 → 本機上架（2026-08-28）

> 給本機 Claude Code 與店主的完整交接。這份文件假設讀者**沒有**遠端工作階段的任何上下文。
> 分支：`claude/batch-c29-album-onboarding-oolzb1`（draft PR #2），領先 main 57 個 commit。
> 遠端環境從未寫入任何線上資料——KV、Firestore、`seed_cards.json`、`apex_pool.json` 全未動，
> 每張卡的 `published` 五旗標都是 false。**上架的每一步都在本機做。**

---

## 一、現況總覽

分支上共 **24 份 `onboarding-manifest-*.json`、3,793 張卡**，每份都通過
`node scripts/verify-album-onboarding.mjs <manifest> --prepare` 的 **0 error** 門檻
（warning 皆為老盤查無條碼，規則明文允許留空），並與當時的全池鍵集去重（零撞鍵）。

| 張數 | 試聽 ready | 頂點候選 | manifest |
|---:|---:|---:|---|
| 1 | 1 | 0 | add-20260816-lishuangze |
| 1 | 1 | 1 | add-20260816 |
| 15 | 15 | 3 | c29-blues-20260821 |
| 326 | 266 | 11 | c30-blues-20260821 |
| 530 | 357 | 36 | c31-world-20260821 |
| 334 | 242 | 10 | c32-folk-20260821 |
| 60 | 27 | 11 | c33-comps-20260821 |
| 186 | 168 | 8 | c34-canon-20260821 |
| 148 | 88 | 7 | c34-japan-20260821 |
| 130 | 94 | 2 | c34-jazz-20260821 |
| 25 | 20 | 0 | c35-shop-reels-20260821 |
| 27 | 23 | 3 | c36-reels-20260822 |
| 349 | 297 | 13 | c37-jazz-20260823 |
| 273 | 230 | 6 | c38-soul-20260823 |
| 232 | 189 | 6 | c39-funk-20260823 |
| 257 | 178 | 15 | c40-electronic-20260823 |
| 111 | 105 | 21 | c41-chinese-20260823 |
| 84 | 75 | 7 | c42-japan-20260823 |
| 54 | 52 | 11 | c43-korea-20260823 |
| 219 | 216 | 11 | c44-rock-20260823 |
| 136 | 129 | 2 | c45-hiphop-20260823 |
| 230 | 194 | 32 | c46-soundtrack-cross-20260828 |
| 52 | 45 | 2 | ctw-taiwan-indie-20260823 |
| 13 | 11 | 0 | ctw2-taiwan-artists-20260823 |
| **3,793** | **3,022** | **218** | 合計 |

哪幾份已經上架過，以店主本機的實際狀態為準——`publish-manifest.mjs` 是冪等的，
乾跑的「已在池中略過」欄會直接告訴你答案，不用人工對帳。

---

## 二、本機上架流程（每份 manifest 走一次）

**入口腳本：`scripts/publish-manifest.mjs`**（2026-08-28 新增，取代逐張手工操作）。
它把 `ALBUM_ONBOARDING.md` §8 的順序固定下來，並刻意**不讓試聽與上架開關同一次寫入**
——前面資料沒寫完就推 seed，卡片會以缺圖缺簡介的狀態曝光。

```bash
# 第 0 步：拉分支、重驗（本機網路環境不同，值得重跑一次）
git fetch origin && git checkout claude/batch-c29-album-onboarding-oolzb1
node scripts/verify-album-onboarding.mjs <manifest.json>

# 第 1 步：乾跑，看數量、產 payload（不動任何檔案）
node scripts/publish-manifest.mjs <manifest.json>
#   → publish-stage/<批名>/card-catalog-patches.json   （Firestore PATCH payload，含 updateMask）
#   → publish-stage/<批名>/preview-static-additions.json
#   → publish-stage/<批名>/preview-status-additions.json

# 第 2 步：Firestore card_catalog（用 payload 逐筆 PATCH；updateMask 已備好，
#          不會蓋掉後台手動改過的欄位）

# 第 3 步：Worker KV 固定簡介
node ../dip-vinyl-worker/scripts/desc-gen/from_onboarding_manifest.mjs <manifest.json> kv-bulk.json
npx wrangler kv bulk put kv-bulk.json --namespace-id 5f65e74b17d644b68a3f542b08a5c105

# 第 4 步：固定試聽（靜態路徑，Git 檔）
node scripts/publish-manifest.mjs <manifest.json> --write-preview
node scripts/build-apple-audio-runtime-map.mjs

# 第 5 步：回讀 2–4 確認與 manifest 一致

# 第 6 步：上架開關（最後才做）
node scripts/publish-manifest.mjs <manifest.json> --write-pool
node scripts/build-seed-genres.mjs        # ← 必跑！manifest 沒有 genre 欄，
                                          #    腳本追加時曲風欄留 null，靠這支補。
                                          #    缺欄的卡首頁「類型挑片」抽不到。

# 第 7 步：published gate
#（把 manifest 的 published 旗標補齊後）
node scripts/verify-album-onboarding.mjs <manifest.json> --published

# 收尾：PROJECT_MEMORY 補一筆、git diff 確認範圍、commit、push main
```

**為什麼試聽走靜態路徑不走後台**：`album_overrides` 的規則是 `allow write: if isAdmin()`，
REST 實測 403。§6 明定靜態路徑（`data/apple-audio-map-v1.json` ＋ `card-preview-status.js`）
與後台等價且為預設，git push 即生效。

**腳本的兩個防呆都實測會觸發，不要繞過**：
- `seed_cards.json` 是「一列一行、**結尾無換行**」的自訂排版，不是 `JSON.stringify(o,null,1)`。
  腳本寫回前先驗重建方式逐字相同，不符就中止——貿然用標準 JSON 寫回會把 8,314 行整個重排。
- `apex_pool.json` 驗 indent=1。

**三套鍵規則各不相同，不要互相套用**（腳本內各自實作）：
- Firestore docId：`(artist+'|'+album).toLowerCase().replace(/\//g,'-').trim()`
- 靜態試聽地圖：NFKD 小寫去重音、保留 CJK、**NUL 分隔**（同 `build-apple-audio-map.mjs`）
- 去重比對：同 `scripts/pool-keys.mjs`，**摺 U+2010–2015 連字號**（否則 a‐ha 這類必然漏判）

---

## 三、待店主裁決／本機才能做的事

### 1. 頂點候選 218 張的採用
manifest 只標 `apexAssessment.eligible/tier`，實際寫入 `apex_pool.json` 依規則
仍需本批「明確採用」。`--write-pool` 會把 eligible 的直接放進對應 tier——
如果某張你不想升，先把該卡 manifest 的 `eligible` 改 false 再跑。

**特別留意**：c-46 有多張 classic=5 因遠端 egress 擋掉 AllMusic／Rolling Stone／
Discogs 網頁而湊不到兩個獨立網域證據、只好先作一般卡——不是評價不夠，是證據拿不到。
本機能連那些站時值得重評：Michael Hedges《Aerial Boundaries》、Enya《Watermark》、
Alton Ellis《Mr. Soul of Jamaica》、Fletcher Henderson《A Study in Frustration》、
七張配樂（ゴジラ、Ran、Black Orpheus、Pulp Fiction、Trainspotting、Easy Rider、Bombay）、
Arrested Development、Judas Priest《Sad Wings of Destiny》、Goodbye Babylon 等
（各筆 `apexReason` 都寫明了差哪一條證據）。

### 2. listeners 補值與 pearl 覆核
遠端擋 last.fm，全部 3,793 張的 `ratings.listeners` 都是 null。本機重跑 `/album-rating`
補值後，各批 `apexReason` 標了「疑似 pearl、待 listeners」的卡可回頭改判
（Washington Phillips、Arizona Dranes、Luis Russell、Impact All Stars、
Pauline Anna Strom、Gigi Masin、濱瀬元彦等）。古典卡不適用 listeners 門檻（§0.7）。

### 3. 線上資料的三個既有問題（遠端依規則未動，清單都在 repo）
- **273 張線上嘻哈卡的試聽是淨化版**（25.7%），含《The Blueprint》《DAMN.》《Take Care》。
  清單：`audits/cleaned-previews-hiphop.md`／`.json`。另 4 張 collectionId 已失效。
  管線已修（新批次不會再中），存量要本機換。
- **6 組掛名分裂重複卡**：`audits/pool-artist-name-splits.md`。最要緊兩組是
  **普卡與王牌重複**（The Velvet Underground 首作、Lauryn Hill《The Miseducation》）；
  另坂本龍一《async》在 seed 以兩種掛名各存一張，是已上線的重複卡。
- **封面掃圖等 40+ 張**：`CURATION_WISHLIST.md` 有完整清單（c-TW 26 張、c-46 12 張、
  c-41/42 等 9 張）。研究全做完了，上傳掃圖即可上架，不用重跑。

### 4. 華語相關（店主 2026-08-23 裁示）
香港／台灣電影配樂（倩女幽魂、悲情城市、重慶森林等）與台灣獨立樂團的剩餘目錄，
留到 **c-49 華語高風險批**用中文來源（金馬獎名單、廠牌官網、中文樂評資料庫）做，
走人工身分路線。店主點名清單在 `CURATION_WISHLIST.md` 最上方。

---

## 四、已知陷阱備忘（本機接續作業時最容易踩的）

1. **iTunes lookup 單次最多回 200 軌，超過會靜默截斷、不報錯。**
   Billie Holiday《Lady Day》230 軌會斷在第 9 碟第 15 軌。這很可能是先前
   「大結集漏帶 trackNames」的真正原因。200 軌以上要用 trackId 逐段掃描補齊。
2. **MB 的「最早 release」不能無條件採用**（c-46 一批踩到五次）：可能是宣傳採樣盤
   （The Social Network）、Pseudo-Release（武満徹《Ran》61 軌）、Bootleg
   （Blade Runner 1993）、含宣傳訪談軌的地區版（Pulp Fiction AU 30 軌）、
   後年重發（Andraé Crouch 2003）。比對曲序或定年份前先看 release 的 status 與性質。
3. **跨文字系統掛名分裂，字串正規化摺不掉**，只能人工雙查（漢字一輪、羅馬拼音一輪）：
   `Tan Dun 譚盾` 在 MB 是**簡體「谭盾」**；`浜瀬元彦` 的 MB 藝人實體是
   **異體字「濱瀬元彦」**、RG 掛名卻是 `Motohiko Hamase`——三種寫法各不相同。
   `喜多郎/Kitaro`、`吉村弘/Yutaka Hirose`、`久石譲/久石讓/Joe Hisaishi` 同理。
4. **CAA 的 release-group 層 front 不保證是原盤**——它回該 RG 底下任意一筆 release 的圖。
   c-44～c-46 靠這條抓到宣傳盤套、日版側標、泰盧固語版、2015 重發版等十餘筆錯圖。
   RG 層不對就逐 release 試，還不行才走 Apple（`1000x1000bf.jpg`，來源記 `manual`）。
5. **Apple 的同名雙胞胎**：explicit vs cleaned 只差 `collectionExplicitness` 一欄
   （§6 已強制檢查）；同名續作（Fresh Aire III、Almighty So 2）、Instrumentals 版、
   重錄版（Psycho 1997 McNeely）曲名幾乎相同，必須逐首比曲序＋比軌數。
6. **策展欄（curatorWhy/curatorRisk）是說法不是事實**。c-46 寫作階段推翻了 40+ 處
   （年份、獎項類別、銷量、人員名單），各卡的更正都在 `aliasReview`／`yearNote`。
7. 策展去重一律用 `node scripts/pool-check.mjs "Artist|Album"`，**不要 grep**
   （U+2010 連字號、C locale 重音、apex_pool 是物件非陣列，三個坑都會讓 grep 說謊）。

---

## 五、後續批次（roadmap 見 `MUSIC_DATABASE_ROADMAP.md`）

| 批 | 內容 | 規模 | 備註 |
|---|---|---|---|
| c-47 | 流行／鄉村／拉丁／音樂劇 | ~170 | 音樂劇 cast recording 以**原版卡司優先**（例外：概念盤早於舞台版者，如 Jesus Christ Superstar） |
| c-48 | 古典補完 | ~130 | **擋路石已排除**：組裝器已支援 `composer` 欄（§0.6）與 `ratings.source: manual:classical-rubric`（§0.7），gate 實測通過。評分要點：經典＝**這份錄音**的地位；「發燒天碟正典」與詮釋正典並列有效（TAS 榜、香港 CD 聖經、首版行情） |
| c-49 | 華語高風險 | ~90 | 時代曲、1990 前台語歌、原民客語補完＋華語電影配樂＋台灣獨立樂團剩餘目錄。人工身分路線，可部分留本機做 |

策展 brief 的方法修正（c-46 的教訓）：**先驗證缺口再決定重心**——雷鬼與新世紀兩線
都推翻了 brief 的假設而且都是對的（roots 已滿、缺口在兩端；ambient 已滿、
缺口是 Windham Hill 那條商品品類線）。

---

## 六、其他參考

- 完整的逐批記錄：`PROJECT_MEMORY.md`（最上方三筆是 c-46／c-45／c-44）
- c-46 中間產物快照：`batch-progress/c46/`（含 RESUME.md；批次已完成，此目錄僅供追溯）
- 台灣線正名裁定：`TW_INDIE_NAMING_RULINGS.md`
- 規格：`ALBUM_ONBOARDING.md`（§5.5 白名單、§5.6 合輯、§6 試聽含 explicitness、§8 寫入順序）
